#include <string>
#include <vector>

std::vector<std::string> str_split(const std::string &str,
                                   const std::string &delim,
                                   bool filter_empty = false);